#include "doctest.h"
#include "sources/BinaryTree.hpp"

using namespace ariel;

TEST_CASE("Check insert root"){

    int count = 0;
    while(count < 20){
        CHECK(count == count);
        count++;
    }
}